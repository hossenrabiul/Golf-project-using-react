import React from 'react';

const Buttons = ({title}) => {
    return (
        <div>
            <button className='recomended-btns'>{title}</button>
        </div>
    );
};

export default Buttons;